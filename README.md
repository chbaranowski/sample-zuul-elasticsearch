# Sample Spring Data Elasticsearch

The elasticsearch REST API from the embedded eleasticsearch server 
is also provided over the zuul proxy on the path http://localhost:8080/sp/* 
(sp = search proxy).

